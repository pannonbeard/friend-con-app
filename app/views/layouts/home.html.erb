<!DOCTYPE html>
<html>
  <%= render 'layouts/head' %>

  <body>
    <%= render 'layouts/head_nav' %>
    <div class='hero-banner'> </div>
    <main>
      <div class='container'>  
        <%= yield %>
      </div>
    </main>
  </body>
</html>